<div class="entry-wrapper">
    <div class="entry-card">

        <h2 class="entry-heading">Enter As</h2>

        <a href="/student/login" class="entry-option student">
            login
        </a>

        <a href="/student/signup"
         class="entry-option admin">
            signup
        </a>

    </div>
</div>
<style>
.entry-wrapper {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: linear-gradient(135deg, #f2f4f8, #e6e9f0);
}

/* ============================= */
/* CARD CONTAINER                */
/* ============================= */

.entry-card {
    width: 420px;
    padding: 45px 40px;
    background: linear-gradient(135deg, #1e1e2f, #2b2b40);
    border-radius: 22px;
    box-shadow: 0 30px 70px rgba(0,0,0,0.35);
    text-align: center;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
}

.entry-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 45px 100px rgba(0,0,0,0.55);
}

/* ============================= */
/* HEADING                       */
/* ============================= */

.entry-heading {
    color: #ffffff;
    font-size: 28px;
    font-weight: 600;
    margin-bottom: 35px;
    letter-spacing: 1.5px;
}

/* ============================= */
/* OPTIONS (BUTTON CARDS)        */
/* ============================= */

.entry-option {
    display: block;
    width: 100%;
    padding: 18px;
    margin-bottom: 25px;

    border-radius: 50px;
    text-decoration: none;
    font-size: 18px;
    font-weight: 600;

    transition: 
        transform 0.35s ease,
        box-shadow 0.35s ease,
        background 0.35s ease;
}

/* STUDENT */
.entry-option.student {
    background: linear-gradient(135deg, #ffb703, #fb8500);
    color: #1e1e2f;
    box-shadow: 0 15px 35px rgba(255,183,3,0.45);
}

.entry-option.student:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 0 25px 60px rgba(255,183,3,0.75);
}

/* ADMIN */
.entry-option.admin {
    background: linear-gradient(135deg, #8ecae6, #219ebc);
    color: #ffffff;
    box-shadow: 0 15px 35px rgba(33,158,188,0.45);
}

.entry-option.admin:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 0 25px 60px rgba(33,158,188,0.75);
}
</style>