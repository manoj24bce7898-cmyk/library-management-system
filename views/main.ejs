<div class="enter-container">

  <!-- STUDENT -->
  <a href="/student" class="enter-card">
    <i class="fa-solid fa-user-graduate"></i>
    <h2>Student</h2>
    <p>Access your dashboard, browse books, wishlist & bookings</p>
  </a>

  <!-- ADMIN -->
  <a href="/admin" class="enter-card">
    <i class="fa-solid fa-user-shield"></i>
    <h2>Admin</h2>
    <p>Manage books, users, inventory & reports</p>
  </a>

</div>



<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
}


/* CONTAINER */
.enter-container {
  display: flex;
  gap: 50px;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
    padding: 100px 50px;
    border: 2px solid #ccc;
    border-radius: 2rem;
    background: linear-gradient(135deg, #f4f6fb, #e6e9f0);
    box-shadow: 0 4px 6px rgba(184, 232, 53, 0.1);
    position: relative;
    top: 20px;
}

/* CARD */
.enter-card {
  width: 280px;
  height: 320px;
  background: #ffffff;
  border-radius: 24px;
  box-shadow: 0 20px 50px rgba(0,0,0,0.15);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-decoration: none;
  color: #1f2937;
  transition: all 0.45s ease;
  position: relative;
  overflow: hidden;
}

/* GLOW EFFECT */
.enter-card::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, #10b981, #059669);
  opacity: 0;
  transition: 0.4s;
}

.enter-card:hover::before {
  opacity: 0.08;
}

/* ICON */
.enter-card i {
  font-size: 54px;
  color: #059669;
  margin-bottom: 20px;
  z-index: 2;
}

/* TITLE */
.enter-card h2 {
  font-size: 22px;
  font-weight: 600;
  margin-bottom: 10px;
  z-index: 2;
}

/* TEXT */
.enter-card p {
  font-size: 14px;
  color: #6b7280;
  text-align: center;
  padding: 0 20px;
  z-index: 2;
}

/* HOVER */
.enter-card:hover {
  transform: translateY(-14px) scale(1.05);
  box-shadow: 0 35px 80px rgba(0,0,0,0.25);
}

.enter-card:hover i {
  color: #047857;
}

/* MOBILE */
@media (max-width: 700px) {
  .enter-container {
    flex-direction: column;
  }
}
</style>
